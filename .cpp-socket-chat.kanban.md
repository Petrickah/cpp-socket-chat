<!-- This is a Kanban Board file created with MD Kanban extension -->
<!-- GitHub: https://github.com/jebakumarj/md-kanban -->
<!-- VS Code Extension: Search "MD Kanban" in the extension store (ID: jeddak.md-kanban) -->

# cpp-socket-chat — Kanban

Task-level progress for this repo, tracked in-repo so it's available on any dev machine. See `CLAUDE.md` for architecture/methodology and `README.md` for the project overview.

## Backlog

#### Task 1.3 — IRC subset: framing + core commands
<!-- id: task-1789400000000-4 -->
Real subset of IRC (RFC 2812 / modern.ircdocs.horse): `\r\n`-terminated frames (512 bytes max including CRLF), partial-buffer handling across `recv()` calls. `NICK`/`USER`/`JOIN`/`PRIVMSG`/`QUIT`, `PING`/`PONG`, numeric replies (001-004 on login, 433 on duplicate nick, 421 on unknown command), `CAP LS` answered with an empty `CAP * LS :` so real clients don't hang. Broadcast to a shared default channel.
Tags: `wave-1` `sockets` `protocol`

#### Task 1.4 — Debugging session + sanitizers
<!-- id: task-1789400000000-5 -->
Confirm `make sanitize` is clean (no leaks, no use-after-free) once there's real logic to sanitize. Documented `gdb` session (breakpoint, variable inspection, backtrace on a real or induced crash) saved as `gdb-session.md`.
Tags: `wave-1` `debugging`

#### Task 1.5 — Validate against a real IRC client
<!-- id: task-1789400000000-6 -->
Connect irssi/hexchat/weechat to `localhost:6667`, confirm login, `JOIN #chat`, and bidirectional messaging with the project's own client. Document any protocol discrepancy found.
Tags: `wave-1` `protocol` `validation`

#### Task 1.6 — README + interview prep pass
<!-- id: task-1789400000000-7 -->
Expand `README.md` with the real architecture writeup and the key design trade-offs (select() vs. poll(), event loop vs. thread-per-client, blocking I/O). Currently just a scaffold overview.
Tags: `wave-1` `docs`

#### Wave 2 — Windows GUI client (ImGui/Walnut) + CMake
<!-- id: task-1789400000000-8 -->
Windows client with an ImGui/Walnut GUI, connecting to the Wave 1 Linux server. Adds CMake as a secondary build system (Makefile stays). Introduces Winsock vs. POSIX sockets and non-blocking network I/O inside the ImGui event loop. Not decomposed into microtasks yet — deferred until Wave 1 is done and presentable. Decoupled from Wave 1's own timeline.
Tags: `wave-2` `not-started`

#### Wave 3 — Containerization + K8s deploy
<!-- id: task-1789400000000-9 -->
Dockerfile + Jenkins Kaniko pipeline + K8s Deployment/Service (NodePort ~30081), dedicated ServiceAccount, same reusable pattern as `code/blog`. Not in the original job posting's requirements — personal curiosity extension. Not decomposed yet, deferred until Wave 1 is done.
Tags: `wave-3` `not-started`

## In Progress

## Paused

## Done

#### Task 1.2 — Multi-client via select()
<!-- id: task-1789400000000-3 -->
Server event loop over the listening socket + all client fds (`select()`): accept new connections, read from clients, detect disconnect (EOF/`recv() == 0`) and clean up the fd. Still echo, not broadcast. Client left single-connection/synchronous — no multiplexing need yet. Debugging session write-up in `docs/task-1.2-multi-client-select.md`.
Tags: `wave-1` `sockets`

#### Task 1.1 — Setup + echo TCP
<!-- id: task-1789400000000-2 -->
Real socket code replacing the placeholder stubs: `socket`/`bind`/`listen`/`accept`/`connect`, bidirectional TCP echo between server and client on localhost. Not yet multi-client. Debugging session write-up in `docs/task-1.1-echo-tcp.md`.
Tags: `wave-1` `sockets`

#### Scaffold — repo, build, CI mirror
<!-- id: task-1789400000000-1 -->
Directory layout (`server/`, `client/`), Makefile (default + `sanitize` targets), placeholder `main.cpp` stubs that build and run, README/CLAUDE.md/LICENSE. Gitea repo (private, source of truth) + public GitHub mirror wired via Jenkins (`Checkout` + `Mirror to GitHub`, SSH deploy key) — verified end-to-end, signed commits land on GitHub. No socket code yet.
Tags: `infra`

